name: Azure Static Web Apps CI/CD TEST

on:
  push:
    branches:
      - test
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - test

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    env:
        GTM: ${{ vars.GTM_ID_TEST }}
        ROOT_URL: ${{ vars.ROOT_URL_TEST }}
        SEARCH_URL: ${{ vars.SEARCH_URL_TEST }}
        UCFHB_SESSION: ${{ vars.UCFHB_SESSION_TEST }}
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: true
          lfs: false
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - name: Build
        run: npm ci && npm run build
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_OCEAN_0501B9D0F }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          # With skip_app_build, app_location IS the build output directory and
          # output_location must be empty. See:
          # https://learn.microsoft.com/en-us/azure/static-web-apps/build-configuration
          app_location: "dist" # Built above by `npm run build`
          # The managed Functions app in `api/`. Static Web Apps builds it
          # itself (npm install + npm run build) — `skip_app_build` above only
          # covers the static side, which we prebuild.
          #
          # Runtime settings for it (ALLOWED_ORIGIN_SUFFIXES, AUTH_VERIFIER,
          # SESSION_TTL...) are NOT set here. They are Static Web App
          # application settings, applied by scripts/api-settings.sh, because a
          # deploy should not be able to silently widen the CORS allowlist.
          api_location: "api"
          output_location: "" # Must be empty when skip_app_build is true
          skip_app_build: true
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_GRAY_OCEAN_0501B9D0F }}
          action: "close"
