node_modules/
dist/
.env

# Playwright
test-results/
playwright-report/
blob-report/

# OS / editor cruft
.DS_Store
Thumbs.db
._*
*~

# Visual baselines are authoritative only from the pinned Playwright container
# (npm run test:visual:docker). Locally-generated macOS ones are smoke-test
# scratch — CI never reads them.
tests/**/*-darwin.png

# Benchmark: vendored v3 assets, generated host pages, raw results
.bench/
